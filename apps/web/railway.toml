[build]
builder = "nixpacks"
buildCommand = "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @velo/web build"

[deploy]
startCommand = "node_modules/.bin/next start"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
