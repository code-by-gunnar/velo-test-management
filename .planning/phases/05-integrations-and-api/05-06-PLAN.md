---
phase: 05-integrations-and-api
plan: 06
type: execute
wave: 5
depends_on:
  - "05-02"
  - "05-04"
  - "05-05"
files_modified: []
autonomous: false
requirements:
  - INT-01
  - INT-02
  - INT-03
  - INT-04

must_haves:
  truths:
    - "All 4 INT requirements verified end-to-end"
    - "Human approval confirms all success criteria met"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 5 features end-to-end. Covers Linear integration, REST API parity, and outbound webhooks.

Purpose: Final gate before marking Phase 5 complete. All 4 INT requirements must be verified by a human tester.
Output: Human approval or list of issues to fix.
</objective>

<execution_context>
@/Users/gunnar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gunnar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-integrations-and-api/05-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify INT-01 — Linear issue creation from failed run item</name>
  <action>
Verify the complete Linear integration flow for defect filing.
  </action>
  <verify>
1. Go to workspace Settings → Integrations tab
2. Click "Connect Linear" — verify redirect to Linear OAuth
3. Authorize on Linear — verify redirect back to Velo settings
4. Select a default team — verify connection status shows org name, team, and date
5. Navigate to a test run and execute a case with "Fail" verdict
6. In the DefectPrompt, enter a title and press Enter/File
7. Verify the defect appears with a Linear badge showing the issue URL
8. Click the badge — verify it opens the correct Linear issue in a new tab
9. Verify the Linear issue has the correct title and description
10. Go back to Settings → Integrations, click "Disconnect" — verify connection removed
11. File another defect — verify it saves locally without error (no Linear badge)
  </verify>
  <done>INT-01 verified. Linear issue creation works from failed run items. Graceful degradation when disconnected. Type "approved" or describe issues.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify INT-02 — Linear status sync</name>
  <action>
Verify that Linear issue status changes appear in the Velo run view.
  </action>
  <verify>
1. With Linear connected, file a defect from a failed run item (creates Linear issue)
2. In Linear, change the issue status (e.g., from "Todo" to "In Progress")
3. Return to the Velo run detail view — verify the defect badge updates to show "In Progress"
4. In Linear, move the issue to "Done" — verify badge updates to "Done" with green color
5. Verify the update happens within a few seconds (webhook-driven, not polling)
6. File two defects quickly — verify both get independent Linear issues and status tracking
  </verify>
  <done>INT-02 verified. Linear status sync works. Badges update in real time via webhook. Type "approved" or describe issues.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify INT-03 — REST API full parity</name>
  <action>
Verify that all UI operations are available via the REST API with API key auth.
  </action>
  <verify>
1. Create an API key from Settings → API Keys
2. Using curl or similar, test API key auth:
   ```
   curl -H "Authorization: Bearer {API_KEY}" {API_URL}/api/v1/workspaces/{WID}/projects
   ```
   Verify it returns the project list
3. Test CRUD gap routes with API key:
   - PATCH a project name
   - GET workspace members
   - PATCH workspace settings
4. Test existing routes via /api/v1/:
   - List test cases
   - List runs
   - Create a test case via API
5. Test rate limiting:
   - Send 101 requests in quick succession
   - Verify the 101st returns 429 with Retry-After header
6. Verify session-based requests (browser) are NOT rate-limited
7. Test with a revoked API key — verify 403 response
  </verify>
  <done>INT-03 verified. Full API parity via /api/v1/ with API key auth. Rate limiting works. Type "approved" or describe issues.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify INT-04 — Outbound webhooks</name>
  <action>
Verify that webhooks fire on key events and are configurable per project.
  </action>
  <verify>
1. Go to Project Settings → Webhooks tab
2. Create a webhook endpoint (use https://webhook.site or similar for testing)
3. Select events: run.completed and run_item.failed
4. Verify the signing secret is shown once — copy it
5. Click "Test" — verify webhook.site receives the test ping payload
6. Verify the X-Velo-Signature header is present on the test payload
7. Execute a test run — fail one case
8. Check webhook.site — verify a `run_item.failed` event was received
9. Complete the entire run — verify a `run.completed` event was received
10. Verify payload contains: run_id, event type, timestamp, and relevant data
11. Deactivate the webhook — repeat a failure — verify NO webhook fires
12. Delete the webhook — verify it's removed from the list
  </verify>
  <done>INT-04 verified. Webhooks fire on configured events with HMAC signing. CRUD and test ping work. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
- All 4 INT requirements verified end-to-end by human tester
- No critical bugs found during verification
</verification>

<success_criteria>
- INT-01: Linear issue filed from failed run item, badge with URL shown
- INT-02: Linear status changes reflected in Velo run view in real time
- INT-03: All resources readable/writable via /api/v1/ with API key auth
- INT-04: Webhooks fire on run.completed and run_item.failed with HMAC signatures
- Human tester approves all 4 verification tasks
</success_criteria>

<output>
After completion, create `.planning/phases/05-integrations-and-api/05-06-SUMMARY.md`
Update `.planning/ROADMAP.md` to mark Phase 5 as complete.
</output>
