---
phase: 05-integrations-and-api
plan: 05
type: execute
wave: 4
depends_on:
  - "05-03"
  - "05-04"
files_modified:
  - apps/web/src/pages/app/[slug]/settings.tsx
  - apps/web/src/components/settings/IntegrationsPanel.tsx
  - apps/web/src/components/settings/LinearConnect.tsx
  - apps/web/src/pages/app/[slug]/[projectKey]/settings.tsx
  - apps/web/src/components/settings/WebhookSettings.tsx
  - apps/web/src/components/runs/DefectPrompt.tsx
  - apps/web/src/components/runs/DefectBadge.tsx
  - apps/web/src/components/layout/Sidebar.tsx
autonomous: false
requirements:
  - INT-01
  - INT-02
  - INT-04

must_haves:
  truths:
    - "User can connect Linear from workspace settings Integrations tab"
    - "User can select a default Linear team during connection"
    - "User can see Linear connection status and disconnect"
    - "DefectPrompt files to Linear automatically when connected (no extra step)"
    - "Defect badges in run view show Linear issue status with color coding"
    - "User can configure webhook endpoints in project settings"
    - "User can test webhook delivery with a ping button"
  artifacts:
    - path: "apps/web/src/components/settings/IntegrationsPanel.tsx"
      provides: "Integrations tab in workspace settings"
      min_lines: 40
    - path: "apps/web/src/components/settings/LinearConnect.tsx"
      provides: "Linear OAuth connect/disconnect UI"
      min_lines: 60
    - path: "apps/web/src/components/settings/WebhookSettings.tsx"
      provides: "Webhook configuration UI in project settings"
      min_lines: 80
    - path: "apps/web/src/components/runs/DefectBadge.tsx"
      provides: "Inline badge showing Linear issue status"
      min_lines: 20
  key_links:
    - from: "apps/web/src/components/settings/LinearConnect.tsx"
      to: "/api/backend/workspaces/:wid/linear/auth"
      via: "fetch via Next.js gateway"
      pattern: "api/backend.*linear"
    - from: "apps/web/src/components/settings/WebhookSettings.tsx"
      to: "/api/backend/workspaces/:wid/projects/:pid/webhooks"
      via: "fetch via Next.js gateway"
      pattern: "api/backend.*webhooks"
---

<objective>
Build all frontend UI for Phase 5: Linear integration settings (connect/disconnect, team selection), updated DefectPrompt for Linear auto-filing, defect status badges in run views, and webhook configuration UI in project settings.

Purpose: Without UI, users cannot connect Linear, configure webhooks, or see Linear issue status. This makes all INT-01, INT-02, and INT-04 features accessible.
Output: IntegrationsPanel, LinearConnect, WebhookSettings, DefectBadge components + updated DefectPrompt.
</objective>

<execution_context>
@/Users/gunnar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gunnar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-integrations-and-api/05-CONTEXT.md
@apps/web/src/pages/app/[slug]/settings.tsx
@apps/web/src/components/runs/DefectPrompt.tsx
@apps/web/src/components/layout/Sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Linear integration settings UI</name>
  <files>
    apps/web/src/pages/app/[slug]/settings.tsx,
    apps/web/src/components/settings/IntegrationsPanel.tsx,
    apps/web/src/components/settings/LinearConnect.tsx
  </files>
  <action>
1. Create `apps/web/src/components/settings/LinearConnect.tsx`:
   - On mount, fetch `GET /api/backend/workspaces/{wid}/linear/status`
   - **Not connected state:**
     - "Connect Linear" button (Cobalt primary button)
     - Clicking calls `GET /api/backend/workspaces/{wid}/linear/auth` and redirects to the returned URL
     - Brief description: "Connect your Linear workspace to file defects directly from failed test runs."
   - **Team selection state** (after OAuth callback):
     - Dropdown listing teams returned from callback
     - "Confirm" button calls `POST /api/backend/workspaces/{wid}/linear/team` with selected team_id
   - **Connected state:**
     - Show: org name, team name, connected date, connected by
     - "Disconnect" button (destructive variant) calls `DELETE /api/backend/workspaces/{wid}/linear/disconnect`
     - Confirmation dialog before disconnect: "This will stop syncing defects with Linear. Existing links will be preserved."

2. Create `apps/web/src/components/settings/IntegrationsPanel.tsx`:
   - Card container with "Integrations" heading
   - Renders LinearConnect component
   - Placeholder text for future integrations: "More integrations coming soon" (small muted text)

3. Update `apps/web/src/pages/app/[slug]/settings.tsx`:
   - Add tab navigation: "General" | "API Keys" | "Integrations"
   - "General" tab shows workspace name/slug settings (existing or stub)
   - "API Keys" tab shows existing ApiKeysPanel
   - "Integrations" tab shows IntegrationsPanel
   - Default to "General" tab. Highlight active tab with Cobalt underline.

Note: OAuth callback handling — after Linear redirects back, the browser lands on a callback URL. Handle this either:
- Via a dedicated `/app/[slug]/settings/linear/callback` page that captures the code and calls the API, OR
- By checking for `?code=` query param on the settings page and auto-submitting to the API
Choose whichever is cleaner. The flow should feel seamless — user clicks "Connect", authorizes on Linear, returns to settings page with connection complete.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>Settings page has 3 tabs. Integrations tab shows LinearConnect with connect/disconnect flow. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Update DefectPrompt and add DefectBadge</name>
  <files>
    apps/web/src/components/runs/DefectPrompt.tsx,
    apps/web/src/components/runs/DefectBadge.tsx
  </files>
  <action>
1. Update `apps/web/src/components/runs/DefectPrompt.tsx`:
   - Remove "Linear integration coming soon" text
   - No other changes needed to the form — the API now handles Linear filing automatically
   - Optionally: after successful defect creation, if the response includes `external_url`, show a small success toast: "Defect filed to Linear" with a link icon
   - The keyboard flow (Enter to file) remains unchanged — zero extra steps for the user

2. Create `apps/web/src/components/runs/DefectBadge.tsx`:
   - Small inline badge component that shows Linear issue status
   - Props: `externalUrl?: string, externalStatus?: string, externalId?: string`
   - If no external_url, render nothing (defect is local-only)
   - If external_url exists, render a clickable badge:
     - Text: status name (e.g., "Todo", "In Progress", "Done")
     - Color coding: "Todo"/"Backlog" = slate, "In Progress"/"Started" = cobalt, "Done"/"Completed" = pass-green, "Cancelled" = muted
     - Clicking opens the Linear issue URL in a new tab
     - Small Linear icon (or chain-link icon) before the text
   - Size: compact, fits inline next to defect chips in the run detail view

3. Integrate DefectBadge into the run detail view:
   - In the component that renders defect chips on failed run items (likely in the run detail page or execution screen), add DefectBadge next to each defect that has external data
   - The badge updates via SSE — when Linear webhook updates external_status, the SSE channel publishes the change, and the badge re-renders

Note: The SSE integration for badge updates may require adding external_status to the SSE payload. If the current SSE message shape doesn't include defect status, extend it.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>DefectPrompt updated (no "coming soon" text). DefectBadge shows Linear status inline with color coding. Integrated into run detail view. Compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Webhook configuration UI in project settings</name>
  <files>
    apps/web/src/pages/app/[slug]/[projectKey]/settings.tsx,
    apps/web/src/components/settings/WebhookSettings.tsx
  </files>
  <action>
1. Create `apps/web/src/components/settings/WebhookSettings.tsx`:
   - On mount, fetch `GET /api/backend/workspaces/{wid}/projects/{pid}/webhooks`
   - **Webhook list:** table with endpoint URL, events (chips), active toggle, actions (edit, delete, test)
   - **Create webhook form:**
     - "Add Webhook" button reveals inline form
     - Endpoint URL input (required, validated as HTTPS URL)
     - Event type checkboxes: `run.completed`, `run_item.failed` (both checked by default)
     - "Create" button calls POST, returns secret
     - Show secret once in highlighted box with copy button (same pattern as API keys)
   - **Edit:** inline edit for endpoint URL and event toggles (PATCH)
   - **Delete:** confirmation dialog, then DELETE
   - **Test ping:** "Test" button on each webhook calls POST .../test, shows inline result:
     - Green checkmark + status code on success
     - Red X + status code + "Failed to deliver" on failure
     - Spinner during the test (up to 10s timeout)
   - **Empty state:** "No webhooks configured. Add one to receive notifications when runs complete or tests fail."

2. Create or update `apps/web/src/pages/app/[slug]/[projectKey]/settings.tsx`:
   - If this page doesn't exist, create it with getServerSideProps + requireAuth
   - Tab structure: "General" | "Webhooks"
   - "General" tab: project name, project key (stub or existing)
   - "Webhooks" tab: WebhookSettings component
   - Page title: "Project Settings"

3. Update Sidebar.tsx:
   - Add "Settings" nav item under project context (below Ingestion)
   - Links to `/app/{slug}/{projectKey}/settings`
   - Gear icon

Note: All fetches use the Next.js gateway pattern (/api/backend/...). Follow existing UI patterns for buttons, cards, forms, and status badges.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>Project settings page with webhooks tab. Create, list, edit, delete, test ping all working. Sidebar updated. Compiles.</done>
</task>

</tasks>

<verification>
- `pnpm --recursive lint && pnpm --recursive typecheck` — no errors
- `cd apps/api && pnpm test` — all tests pass
- Settings page has 3 tabs (General, API Keys, Integrations)
- Linear connect/disconnect flow renders correctly
- DefectBadge shows Linear status inline
- Project settings page has webhook configuration
- All fetches use /api/backend/ gateway pattern
</verification>

<success_criteria>
- Workspace settings: Integrations tab with Linear connect/disconnect
- Linear OAuth flow: click → authorize → select team → connected (30-second setup)
- DefectPrompt: no "coming soon" text, auto-files to Linear silently
- DefectBadge: inline status badge with color coding and click-to-open
- Project settings: webhook CRUD with test ping
- Sidebar: project settings link added
- Full CI simulation passes: lint + typecheck + tests
</success_criteria>

<output>
After completion, create `.planning/phases/05-integrations-and-api/05-05-SUMMARY.md`
</output>
