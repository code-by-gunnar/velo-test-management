---
phase: 04-ci-ingestion
plan: 05
type: execute
wave: 4
depends_on:
  - "04-04"
files_modified:
  - apps/web/src/pages/app/[slug]/settings.tsx
  - apps/web/src/components/settings/ApiKeysPanel.tsx
  - apps/web/src/pages/app/[slug]/[projectKey]/ingestion.tsx
  - apps/web/src/components/ingestion/IngestionHistory.tsx
  - apps/web/src/components/ingestion/SetupGuide.tsx
  - apps/web/src/components/layout/Sidebar.tsx
autonomous: false
requirements:
  - IN-01
  - IN-04

must_haves:
  truths:
    - "User can create an API key from workspace settings and see the raw key once"
    - "User can see list of API keys with prefix and created date (never the full key)"
    - "User can revoke an API key"
    - "User can see ingestion history for a project with status, counts, and timestamps"
    - "User can copy a curl command for CI pipeline setup"
    - "Sidebar shows Ingestion nav item under project context"
  artifacts:
    - path: "apps/web/src/components/settings/ApiKeysPanel.tsx"
      provides: "API key management UI component"
      min_lines: 60
    - path: "apps/web/src/pages/app/[slug]/[projectKey]/ingestion.tsx"
      provides: "Ingestion history page"
      min_lines: 40
    - path: "apps/web/src/components/ingestion/SetupGuide.tsx"
      provides: "curl command template for CI setup"
      min_lines: 30
  key_links:
    - from: "apps/web/src/components/settings/ApiKeysPanel.tsx"
      to: "/api/backend/workspaces/:wid/api-keys"
      via: "fetch via Next.js gateway"
      pattern: "api/backend.*api-keys"
    - from: "apps/web/src/pages/app/[slug]/[projectKey]/ingestion.tsx"
      to: "/api/backend/workspaces/:wid/projects/:pid/ingestion-runs"
      via: "fetch via Next.js gateway"
      pattern: "api/backend.*ingestion-runs"
---

<objective>
Build the frontend UI for API key management (in workspace settings) and ingestion history (per project). Includes a setup guide with copyable curl commands so users can configure their CI pipelines.

Purpose: Without UI, users cannot create API keys or see ingestion results. This makes the CI ingestion feature accessible to non-CLI users.
Output: ApiKeysPanel in settings, ingestion history page, setup guide with curl templates.
</objective>

<execution_context>
@/Users/gunnar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gunnar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ci-ingestion/04-RESEARCH.md
@apps/web/src/pages/app/[slug]/settings.tsx
@apps/web/src/components/layout/Sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: API key management panel in workspace settings</name>
  <files>apps/web/src/pages/app/[slug]/settings.tsx, apps/web/src/components/settings/ApiKeysPanel.tsx</files>
  <action>
1. Create `apps/web/src/components/settings/ApiKeysPanel.tsx`:
   - Fetches GET /api/backend/workspaces/{wid}/api-keys on mount
   - Displays table of keys: name, prefix (e.g. "velo_abc..."), created date, status (active/revoked)
   - "Create API Key" button opens inline form: name input + Create button
   - On POST success, show the raw key in a highlighted box with a "Copy" button and warning: "This key will only be shown once. Copy it now."
   - After dismissing the key display, it disappears forever (only prefix shown in table)
   - Revoke button on each active key — calls DELETE, sets revoked_at, grays out the row
   - Use existing design system components (Button, Card, form inputs)
   - Follow the Velo design language: Cobalt accent, Mist background, Inter font

2. Add ApiKeysPanel to the existing `settings.tsx` page:
   - Add a new section/tab "API Keys" below existing settings content
   - Import and render ApiKeysPanel with workspaceId from session

Note: All fetches go through the Next.js gateway pattern (`/api/backend/...`). Never call the Railway API directly from the browser.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>Settings page shows API Keys section. User can create, view (prefix only), and revoke keys. Raw key shown once on creation with copy button.</done>
</task>

<task type="auto">
  <name>Task 2: Ingestion history page + setup guide + sidebar nav</name>
  <files>
    apps/web/src/pages/app/[slug]/[projectKey]/ingestion.tsx,
    apps/web/src/components/ingestion/IngestionHistory.tsx,
    apps/web/src/components/ingestion/SetupGuide.tsx,
    apps/web/src/components/layout/Sidebar.tsx
  </files>
  <action>
1. Create `apps/web/src/components/ingestion/SetupGuide.tsx`:
   - Render a card with instructions for CI pipeline setup
   - Show curl command templates for JUnit XML and Allure JSON:
     ```
     curl -X POST https://velo-test-management-production.up.railway.app/api/workspaces/{WORKSPACE_ID}/projects/{PROJECT_ID}/ingest/junit \
       -H "Authorization: Bearer YOUR_API_KEY" \
       -F "file=@junit-results.xml"
     ```
   - Include similar for Allure JSON (both multipart and JSON body)
   - "Copy" button on each curl template
   - Link to settings page for API key creation if no keys exist yet
   - Use the API base URL from environment or a sensible default

2. Create `apps/web/src/components/ingestion/IngestionHistory.tsx`:
   - Fetches GET /api/backend/workspaces/{wid}/projects/{pid}/ingestion-runs on mount
   - Displays table: timestamp, format (JUnit/Allure), status badge (success/parse_error/partial), total tests, matched tests, unmatched count, link to run detail if run_id exists
   - Status badge uses existing StatusBadge component pattern: success=green, parse_error=red, partial=amber
   - Empty state: "No CI results yet. Set up your pipeline using the guide above."
   - Each row with a run_id links to `/app/{slug}/{projectKey}/runs/{runId}`

3. Create `apps/web/src/pages/app/[slug]/[projectKey]/ingestion.tsx`:
   - Standard page with getServerSideProps using requireAuth
   - Renders SetupGuide at top, then IngestionHistory below
   - Page title: "CI Ingestion"
   - Pass workspaceId, projectId, projectKey from session/params

4. Update Sidebar.tsx:
   - Add "Ingestion" nav item in the project-scoped section (below "Runs")
   - Icon: use an upload/cloud icon from the existing icon set or a simple SVG
   - Links to `/app/{slug}/{projectKey}/ingestion`

Note: Follow existing page patterns from cases.tsx or runs/ pages for layout, auth, and data fetching. Use the Next.js gateway pattern for all API calls.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>Ingestion page shows setup guide with copyable curl commands and ingestion history table. Sidebar shows Ingestion nav item. TypeScript compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete CI ingestion pipeline end-to-end</name>
  <action>
Human verifies the complete CI ingestion pipeline works end-to-end: API key management in workspace settings, JUnit XML and Allure JSON ingestion via REST API, raw payload storage to R2 (or skipped if R2 not configured), auto-mapping of results to test cases by name, ingestion history page with setup guide.
  </action>
  <verify>
1. Go to workspace Settings page — verify "API Keys" section appears
2. Create an API key — verify the raw key is shown once with a copy button
3. Copy the key, dismiss — verify only the prefix is shown in the table
4. Navigate to a project's "Ingestion" page via sidebar
5. Verify the setup guide shows curl commands with correct URLs
6. (If R2 is configured) Run the curl command from terminal with a JUnit XML file:
   curl -X POST {API_URL}/api/workspaces/{WID}/projects/{PID}/ingest/junit -H "Authorization: Bearer {YOUR_KEY}" -F "file=@test-results.xml"
7. Verify response contains run_id, total_tests, matched_tests
8. Navigate to Runs page — verify a new "CI: JUnit Import" run appears
9. Check ingestion history page — verify the ingestion record appears with success status
10. Revoke the API key in settings — verify it appears grayed out
  </verify>
  <done>All 10 verification steps pass. CI ingestion pipeline works end-to-end. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
- `pnpm --recursive lint && pnpm --recursive typecheck` — no errors
- `cd apps/api && pnpm test` — all tests pass
- Settings page renders API Keys panel
- Ingestion page renders setup guide and history
- Sidebar shows Ingestion link
</verification>

<success_criteria>
- API keys can be created, displayed (prefix only), and revoked from the UI
- Raw key shown only once at creation with copy-to-clipboard
- Ingestion history shows all past CI pushes with status and counts
- Setup guide provides correct curl commands for both JUnit and Allure
- Sidebar navigation includes Ingestion link under project context
- Full CI simulation passes: lint + typecheck + tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-ci-ingestion/04-05-SUMMARY.md`
</output>
