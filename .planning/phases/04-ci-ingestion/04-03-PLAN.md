---
phase: 04-ci-ingestion
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - apps/api/src/lib/allure-parser.ts
  - apps/api/src/lib/__tests__/allure-parser.test.ts
  - apps/api/src/lib/r2.ts
autonomous: true
requirements:
  - IN-02
  - IN-04

must_haves:
  truths:
    - "Allure JSON parser maps passed/failed/broken/skipped to Velo statuses"
    - "Allure parser handles both single object and array inputs"
    - "R2 client uploads buffers and generates presigned download URLs"
    - "R2 env vars are validated at module load (not inside request handler)"
  artifacts:
    - path: "apps/api/src/lib/allure-parser.ts"
      provides: "parseAllureJson function returning NormalizedTestCase[]"
      exports: ["parseAllureJson"]
      min_lines: 30
    - path: "apps/api/src/lib/r2.ts"
      provides: "R2 S3 client singleton, uploadToR2, getR2PresignedUrl, buildR2Key"
      exports: ["r2Client", "uploadToR2", "getR2PresignedUrl", "buildR2Key"]
      min_lines: 40
  key_links:
    - from: "apps/api/src/lib/allure-parser.ts"
      to: "apps/api/src/lib/junit-parser.ts"
      via: "shared NormalizedTestCase interface"
      pattern: "NormalizedTestCase"
---

<objective>
Implement the Allure JSON parser and Cloudflare R2 storage client. The Allure parser converts Allure result JSON to the same NormalizedTestCase interface used by the JUnit parser. The R2 client provides upload and presigned-URL helpers for raw payload storage.

Purpose: These two libraries are consumed by the ingestion routes in plan 04-04. They must exist and be tested before route integration.
Output: Working allure-parser.ts with tests, r2.ts client module.
</objective>

<execution_context>
@/Users/gunnar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gunnar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ci-ingestion/04-RESEARCH.md
@apps/api/src/routes/__tests__/fixtures/allure-result.json

<interfaces>
<!-- From 04-02 output (junit-parser.ts) — Allure parser MUST return same interface -->
```typescript
export interface NormalizedTestCase {
  name: string
  classname: string | null
  fullName: string
  durationMs: number | null
  status: "pass" | "fail" | "skipped"
  failureMessage: string | null
  failureBody: string | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Allure JSON parser with tests</name>
  <files>apps/api/src/lib/allure-parser.ts, apps/api/src/lib/__tests__/allure-parser.test.ts</files>
  <behavior>
    - parseAllureJson('{"uuid":"x","name":"test","status":"passed"}') returns 1 NormalizedTestCase with status "pass"
    - parseAllureJson('[{...passed}, {...failed}, {...broken}, {...skipped}]') returns 4 results with correct status mapping
    - Allure "broken" maps to Velo "fail"; Allure "unknown" maps to Velo "skipped"
    - durationMs computed from stop - start when both present; null when either missing
    - failureMessage from statusDetails.message; failureBody from statusDetails.trace
    - fullName preferred over name for the fullName field; falls back to name then uuid
    - Throws on invalid JSON
    - Returns descriptive 400-style error message when content starts with PK (ZIP magic bytes)
  </behavior>
  <action>
1. RED: Replace todo stubs in allure-parser.test.ts with real test implementations using the allure-result.json fixture. Add tests for: single object, array, status mapping (all 5 Allure statuses), duration computation, failure detail extraction, ZIP detection, invalid JSON error.

2. GREEN: Create `apps/api/src/lib/allure-parser.ts`:
   - Import NormalizedTestCase from `./junit-parser.js` (re-use the interface, do not duplicate)
   - Define AllureResult interface matching the Allure JSON schema from research
   - Implement parseAllureJson(raw: string): NormalizedTestCase[]
   - Implement mapAllureStatus mapping: passed->pass, failed/broken->fail, skipped/unknown->skipped
   - Add ZIP detection: if raw starts with "PK" (ZIP magic bytes), throw Error("Allure ZIP ingestion not supported — send individual *-result.json files")
   - Handle both single object and array inputs via Array.isArray check

3. REFACTOR if needed.
  </action>
  <verify>
    <automated>cd apps/api && npx vitest run src/lib/__tests__/allure-parser.test.ts</automated>
  </verify>
  <done>All 6+ Allure parser tests pass. Status mapping covers all 5 Allure statuses. ZIP detection works. Duration computation correct.</done>
</task>

<task type="auto">
  <name>Task 2: Create R2 storage client module</name>
  <files>apps/api/src/lib/r2.ts</files>
  <action>
Create `apps/api/src/lib/r2.ts` with:

1. **Conditional initialization** — R2 is optional for local dev and tests. Do NOT throw at module load if env vars are missing. Instead:
   - Export a `r2Enabled()` function that returns true if all 4 env vars are set
   - Export `getR2Client()` that lazily creates the S3Client on first call and throws if env vars missing
   - This avoids breaking test and dev environments that do not have R2 configured

2. **S3Client configuration** (created lazily by getR2Client):
   ```
   region: "auto"
   endpoint: `https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com`
   credentials: { accessKeyId: R2_ACCESS_KEY_ID, secretAccessKey: R2_SECRET_ACCESS_KEY }
   ```

3. **uploadToR2(key: string, body: Buffer, contentType: string): Promise<void>** — PutObjectCommand to R2_BUCKET_NAME

4. **getR2PresignedUrl(key: string): Promise<string>** — getSignedUrl with GetObjectCommand, expiresIn: 3600 (1 hour)
   - Import getSignedUrl from `@aws-sdk/s3-request-presigner`

5. **buildR2Key(workspaceId: string, format: "junit" | "allure", ingestionId: string): string** — returns `ingestion/${workspaceId}/${format}/${ingestionId}/payload.${ext}` where ext is "xml" for junit, "json" for allure

All exports: r2Enabled, getR2Client, uploadToR2, getR2PresignedUrl, buildR2Key

Note: No unit tests needed for R2 client (it wraps SDK calls). Integration tests in 04-04 will mock the S3Client.
  </action>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit && pnpm test</automated>
  </verify>
  <done>r2.ts compiles, exports all 5 functions, uses lazy initialization that does not break tests without R2 env vars.</done>
</task>

</tasks>

<verification>
- `cd apps/api && npx vitest run src/lib/__tests__/allure-parser.test.ts` — all tests pass
- `cd apps/api && npx tsc --noEmit` — no type errors
- `cd apps/api && pnpm test` — full suite green
</verification>

<success_criteria>
- Allure parser correctly maps all 5 Allure statuses to Velo statuses
- Allure parser handles both single object and array JSON inputs
- ZIP detection rejects binary content with descriptive error
- R2 client compiles and exports upload/presign/buildKey helpers
- R2 client does not crash at import time without env vars (lazy init)
- NormalizedTestCase interface shared between junit-parser and allure-parser (not duplicated)
</success_criteria>

<output>
After completion, create `.planning/phases/04-ci-ingestion/04-03-SUMMARY.md`
</output>
