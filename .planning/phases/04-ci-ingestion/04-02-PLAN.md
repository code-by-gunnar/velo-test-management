---
phase: 04-ci-ingestion
plan: 02
type: tdd
wave: 2
depends_on:
  - "04-01"
files_modified:
  - apps/api/src/lib/junit-parser.ts
  - apps/api/src/lib/__tests__/junit-parser.test.ts
autonomous: true
requirements:
  - IN-03

must_haves:
  truths:
    - "JUnit parser handles pytest-junit XML without error"
    - "JUnit parser handles Maven Surefire XML (bare testsuite root) without error"
    - "JUnit parser handles Gradle XML with error elements without error"
    - "JUnit parser handles Jest-junit XML without error"
    - "JUnit parser handles Go gotestsum XML without error"
    - "Single-testcase XML does not crash (isArray edge case)"
    - "Both failure and error child elements map to fail status"
  artifacts:
    - path: "apps/api/src/lib/junit-parser.ts"
      provides: "parseJUnitXml function returning NormalizedTestCase[]"
      exports: ["parseJUnitXml", "NormalizedTestCase"]
      min_lines: 50
    - path: "apps/api/src/lib/__tests__/junit-parser.test.ts"
      provides: "Unit tests for all 5 JUnit variants + edge cases"
      min_lines: 80
  key_links:
    - from: "apps/api/src/lib/__tests__/junit-parser.test.ts"
      to: "apps/api/src/lib/junit-parser.ts"
      via: "import { parseJUnitXml }"
      pattern: "parseJUnitXml"
---

<objective>
TDD implementation of the defensive JUnit XML parser that handles all 5 target CI variants (pytest-junit, Maven Surefire, Gradle, Jest-junit, Go gotestsum) without error.

Purpose: IN-03 is the highest-risk requirement — JUnit XML has no normative spec and every tool produces different output. TDD ensures the parser handles all variants before any integration code depends on it.
Output: Working, fully-tested parseJUnitXml() function.
</objective>

<execution_context>
@/Users/gunnar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gunnar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ci-ingestion/04-RESEARCH.md
@apps/api/src/routes/__tests__/fixtures/pytest-report.xml
@apps/api/src/routes/__tests__/fixtures/surefire-report.xml
@apps/api/src/routes/__tests__/fixtures/gradle-report.xml
@apps/api/src/routes/__tests__/fixtures/jest-junit-report.xml
@apps/api/src/routes/__tests__/fixtures/gotestsum-report.xml
@apps/api/src/routes/__tests__/fixtures/single-test-junit.xml
</context>

<feature>
  <name>JUnit XML Parser (5 variants)</name>
  <files>apps/api/src/lib/junit-parser.ts, apps/api/src/lib/__tests__/junit-parser.test.ts</files>
  <behavior>
    - parseJUnitXml(pytestXml) returns 3 NormalizedTestCase objects: 1 pass, 1 fail, 1 skipped
    - parseJUnitXml(surefireXml) returns results from bare testsuite root; maps both failure and error to fail
    - parseJUnitXml(gradleXml) returns 4 results; error child maps to fail status
    - parseJUnitXml(jestXml) returns results with describe-block classnames
    - parseJUnitXml(gotestsumXml) returns results with Go package classnames
    - parseJUnitXml(singleTestXml) returns exactly 1 result (no array collapse crash)
    - parseJUnitXml("<invalid") throws Error with "JUnit XML parse error" message
    - parseJUnitXml("<root/>") returns empty array (unrecognized format, not throw)
    - Each NormalizedTestCase has: name, classname (nullable), fullName, durationMs (number|null), status, failureMessage (nullable), failureBody (nullable)
    - fullName = classname ? `${classname}.${name}` : name
    - durationMs = Math.round(parseFloat(time_attr) * 1000)
  </behavior>
  <implementation>
Use fast-xml-parser with XMLParser configured per research:
- ignoreAttributes: false, attributeNamePrefix: "@_"
- isArray with ALWAYS_ARRAY_PATHS for testsuite.testcase and testsuites.testsuite.testcase and testsuites.testsuite
- cdataPropName: "__cdata"
- Root normalization: check parsed.testsuites first, then parsed.testsuite
- Status: tc.failure || tc.error -> fail; tc.skipped !== undefined -> skipped; else pass
- Export NormalizedTestCase interface and parseJUnitXml function

Follow the complete implementation pattern from 04-RESEARCH.md Pattern 3 / Code Examples section.
  </implementation>
</feature>

<verification>
- `cd apps/api && npx vitest run src/lib/__tests__/junit-parser.test.ts` — all 10 tests pass
- `cd apps/api && pnpm test` — full suite green
</verification>

<success_criteria>
- parseJUnitXml correctly parses all 5 fixture files without error
- Single-test fixture returns 1 result (isArray edge case handled)
- Both failure and error XML children map to "fail" status
- Invalid XML throws descriptive error
- Unrecognized but valid XML returns empty array
- All 10+ tests pass in under 2 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/04-ci-ingestion/04-02-SUMMARY.md`
</output>
